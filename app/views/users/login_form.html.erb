<h1>Welcome to RACENOTE</h1>

<p id="notice"><%= notice %></p>

<% if @error_message %>
  <div class="error_message">
    </<div class="">
      <%= @error_message %>
    </div>
<% end %>

<%= form_tag("/login") do %>
  <p>メールアドレス</p>
  <input name="email" value="<%= @email %>">
  <p>パスワード</p>
  <input type="password" name="password" value="<%= @password %>">
  <input type="submit" value="ログイン">
<% end %>

<p>
<%= link_to '新規ユーザ作成', ("/signup") %>
</p>
